---
- name: Rebuild llama b10612 AI Store
  hosts: local
  connection: local
  gather_facts: false

  vars:
    ai_stores_root: "{{ playbook_dir }}/../.."
    ai_stores_module: llama-b10612

  roles:
    - ai-stores
