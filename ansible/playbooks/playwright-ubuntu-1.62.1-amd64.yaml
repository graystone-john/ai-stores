---
- name: Build Playwright Ubuntu Noble runtime AI Store
  hosts: localhost
  connection: local
  gather_facts: false

  roles:
    - role: ai-stores
      vars:
        ai_stores_module: playwright-ubuntu-1.62.1-amd64
